# Chain to existing resource server + agent config.
config_paths:
  - resources_servers/math_with_judge/configs/math_with_judge.yaml

# Isolated benchmark-specific inheritance.
aime25_x_math_with_judge_resources_server:
  _inherit_from: math_with_judge
  resources_servers:
    math_with_judge:
      # Skills' aime25-x uses eval_type=math, so keep symbolic-only verification.
      should_use_judge: false

aime25_x_math_with_judge_simple_agent:
  _inherit_from: math_with_judge_simple_agent
  responses_api_agents:
    simple_agent:
      resources_server:
        name: aime25_x_math_with_judge_resources_server
      datasets:
      - name: aime25_x
        type: benchmark
        jsonl_fpath: benchmarks/aime25_x/data/aime25_x_benchmark.jsonl
        prompt_config: benchmarks/aime25_x/prompts/default.yaml
        prepare_script: benchmarks/aime25_x/prepare.py
        num_repeats: 32

