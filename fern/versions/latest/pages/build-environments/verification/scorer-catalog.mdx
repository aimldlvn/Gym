---
title: "Scorer Catalog"
description: "Filterable catalog of every verify() pattern shipped across resources_servers/. Filter by pattern, domain, judge-required, deterministic. Each card cites the source verify() line."
content_type: reference
audience_level: intermediate
journey_stage: build
position: 4
---
import { ScorerCatalog } from "../../../../../components/scorers/ScorerCatalog";

Every shipped resources server in `resources_servers/` implements a `verify()` function. This page is the filterable lookup. For the prose explanation of each pattern with copy-this-when criteria and worked examples, see [Scorer Patterns](/latest/build-environments/verification/scorers).

<ScorerCatalog />
